[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-explainable-kt"
version = "0.1.0"
description = "Explainable KT and misconception diagnosis baseline for Lumi"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["hermes_kt", "hermes_practice"]
