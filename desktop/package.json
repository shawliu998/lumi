{
  "name": "lumi-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Lumi local-first Xingce learning agent for macOS.",
  "scripts": {
    "build:frontend": "npm --prefix ../client run build",
    "check:config": "node scripts/check-config.mjs",
    "build:sidecar": "sh scripts/build-sidecar.sh",
    "check:sidecar": "npm run check:config && node scripts/check-sidecar.mjs",
    "check:managed-app": "node scripts/check-managed-app.mjs",
    "dev": "sh scripts/tauri.sh dev",
    "dev:frontend": "npm --prefix ../client run dev -- --host 127.0.0.1 --port 1420 --strictPort",
    "build:fast-app": "npm run build:frontend && npm run check:sidecar && sh scripts/tauri.sh build --debug --bundles app",
    "build:fast-release": "npm run build:frontend && npm run check:sidecar && sh scripts/tauri.sh build --bundles app,dmg",
    "build": "npm run build:frontend && npm run build:sidecar && npm run check:sidecar && sh scripts/tauri.sh build",
    "build:debug-app": "npm run build:frontend && npm run build:sidecar && npm run check:sidecar && sh scripts/tauri.sh build --debug --bundles app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
