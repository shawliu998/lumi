[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-domains"
version = "0.1.0"
description = "Deterministic domain adapters and synthetic learning-loop fixtures"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["hermes_domains"]
